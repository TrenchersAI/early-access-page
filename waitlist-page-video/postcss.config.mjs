const config = {
  plugins: {
    "@tailwindcss/postcss": {},
    /* Adds vendor prefixes for properties used directly in globals.css
       (mask-image, backdrop-filter inside @supports, etc.) per browserslist. */
    autoprefixer: {},
  },
};

export default config;
